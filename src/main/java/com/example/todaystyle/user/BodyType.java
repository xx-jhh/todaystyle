package com.example.todaystyle.user;

/**
 * 회원가입 시 사용자가 선택하는 골격 기반 체형 진단 카테고리(스트레이트/웨이브/내추럴).
 */
public enum BodyType {
    /** 스트레이트: 상체·흉곽에 입체감과 두께가 있고 어깨가 각진 체형. */
    STRAIGHT,
    /** 웨이브: 상체는 얇고 하체(골반)에 볼륨이 집중된 체형. */
    WAVE,
    /** 내추럴: 골격이 크고 관절이 도드라지며 팔다리가 긴 체형. */
    NATURAL
}
