package com.example.todaystyle.user;

/**
 * 회원가입 시 사용자가 이미지들을 보고 선택하는 체형 카테고리.
 * 실제 제시할 체형 이미지 세트가 정해지면 값을 맞춰 조정한다.
 */
public enum BodyType {
    SLIM,
    STANDARD,
    ATHLETIC,
    CHUBBY,
    TALL_SLIM
}
