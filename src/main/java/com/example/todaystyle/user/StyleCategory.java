package com.example.todaystyle.user;

/**
 * 회원가입 시 선택하는 선호 스타일. 최종 카테고리 목록이 정해지면 값을 맞춰 조정한다.
 */
public enum StyleCategory {
    CASUAL,
    AMEKAJI,
    STREET,
    MINIMAL,
    FORMAL,
    VINTAGE
}
