# /var/lib/grafana/dashboards 아래 있는 JSON 파일을 자동으로 대시보드로 불러온다.
apiVersion: 1

providers:
  - name: todaystyle
    folder: todaystyle
    type: file
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
